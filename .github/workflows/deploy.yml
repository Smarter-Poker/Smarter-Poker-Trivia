name: CI Health Check

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify repository
        run: |
          echo "✅ Repository is healthy"
          echo "This module deploys as part of the World Hub monorepo."
          ls -la
